{
  "name": "Appointment",
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date-time"
    },
    "type": {
      "type": "string",
      "enum": [
        "ai_consultation",
        "follow_up",
        "screening",
        "vaccination",
        "checkup",
        "specialist"
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "scheduled",
        "confirmed",
        "completed",
        "cancelled"
      ],
      "default": "pending"
    },
    "notes": {
      "type": "string"
    },
    "provider": {
      "type": "string"
    },
    "family_member_id": {
      "type": "string"
    },
    "consultation_id": {
      "type": "string"
    }
  },
  "required": [
    "title",
    "date"
  ]
}